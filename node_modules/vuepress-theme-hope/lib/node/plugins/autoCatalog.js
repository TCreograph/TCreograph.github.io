import { isPlainObject } from "@vuepress/shared";
import { autoCatalogPlugin } from "vuepress-plugin-auto-catalog";
/**
 * Resolve options for vuepress-plugin-auto-catalog
 */
export const getAutoCatalogPlugin = (autoCatalog) => {
    if (autoCatalog === false)
        return null;
    return autoCatalogPlugin(isPlainObject(autoCatalog) ? autoCatalog : {});
};
//# sourceMappingURL=autoCatalog.js.map
import { entries } from "vuepress-shared/node";
export const prepareThemeColorScss = async (app, themeData) => {
    await app.writeTemp("theme-hope/theme-color.scss", `\
$picker: (${entries(themeData.themeColor || {})
        .map(([color, value]) => `"${color}": ${value}`)
        .join(",")});
`);
};
//# sourceMappingURL=themeColor.js.map